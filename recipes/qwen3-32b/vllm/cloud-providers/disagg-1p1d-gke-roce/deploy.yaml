# SPDX-FileCopyrightText: Copyright (c) 2025-2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0
#
# Qwen3-32B BF16 disagg, 1 prefill + 1 decode, cross-node over GCP A4X (GB200 NVL72) RoCEv2.

apiVersion: nvidia.com/v1alpha1
kind: DynamoGraphDeployment
metadata:
  name: q32b-1p1d-gke-roce
spec:
  pvcs:
  - create: false
    name: shared-model-cache
  services:
    Frontend:
      componentType: frontend
      envs:
        - name: HF_HOME
          value: /shared-models
      extraPodSpec:
        imagePullSecrets:
        - name: nvcr-pull-secret
        # vllm-runtime:1.1.1 has a built-in ENTRYPOINT — overriding command crashes the Rust binary.
        mainContainer:
          image: nvcr.io/nvidia/ai-dynamo/dynamo-frontend:1.1.1
          workingDir: /workspace
          # Without this mount, Dynamo runtime fails to register the model card with
          # "Failed to create cache directory /shared-models/hub: Permission denied"
          # → /v1/models returns {data:[]} and chat completions 404.
          volumeMounts:
          - { name: shared-model-cache, mountPath: /shared-models }
        volumes:
        - { name: shared-model-cache, persistentVolumeClaim: { claimName: shared-model-cache } }
      replicas: 1
      resources:
        requests: { cpu: "4" }
        limits:   { cpu: "8" }
      subComponentType: null

    VllmPrefillWorker:
      componentType: worker
      envFromSecret: hf-token-secret
      extraPodMetadata:
        annotations:
          prometheus.io/scrape: "true"
          prometheus.io/port: "19090"
          prometheus.io/path: "/metrics"
          networking.gke.io/default-interface: "eth0"
          networking.gke.io/interfaces: |
            [{"interfaceName":"eth0","network":"default"},
             {"interfaceName":"eth2","network":"rdma-0"},
             {"interfaceName":"eth3","network":"rdma-1"},
             {"interfaceName":"eth4","network":"rdma-2"},
             {"interfaceName":"eth5","network":"rdma-3"}]
      extraPodSpec:
        imagePullSecrets:
        - name: nvcr-pull-secret
        tolerations:
        - { key: nvidia.com/gpu,                            operator: Exists, effect: NoSchedule }
        - { key: kubernetes.io/arch,                        operator: Equal, value: arm64, effect: NoSchedule }
        mainContainer:
          command: [python3, -m, dynamo.vllm]
          image: nvcr.io/nvidia/ai-dynamo/vllm-runtime:1.1.1
          workingDir: /workspace
          args:
          # Loaded by HF model id; the model is cached on the shared PVC (lustre).
          - --model
          - Qwen/Qwen3-32B
          - --served-model-name
          - Qwen/Qwen3-32B
          - --disaggregation-mode
          - prefill
          - --kv-transfer-config
          - '{"kv_connector":"NixlConnector","kv_role":"kv_both"}'
          - --tensor-parallel-size
          - '4'
          - --no-enable-log-requests
          - --gpu-memory-utilization
          - '0.90'
          - --async-scheduling
          - --block-size
          - '64'
          - --hf-overrides
          - '{"rope_scaling":{"rope_type":"yarn","factor":4.0,"original_max_position_embeddings":32768},"max_position_embeddings":131072}'
          - --max-model-len
          - '131072'
          securityContext:
            privileged: true
            runAsUser: 0
            capabilities:
              add: [IPC_LOCK, SYS_PTRACE]
          env:
          # --- NIXL telemetry (transport-neutral) ---
          - { name: NIXL_TELEMETRY_ENABLE,          value: "y" }
          - { name: NIXL_TELEMETRY_EXPORTER,        value: "prometheus" }
          - { name: NIXL_TELEMETRY_PROMETHEUS_PORT, value: "19090" }
          # --- UCX backend over RoCE (NIXL default; no DYN_KVBM_NIXL_BACKEND_* needed) ---
          # NOTE: don't set UCX_TLS — an explicit transport allowlist can omit the
          # internal transports UCX needs for wireup. UCX_NET_DEVICES is left unset too:
          # on A4X, UCX auto-probe binds the 4 RoCE NICs (rc_mlx5 over mlx5_0..3) on its
          # own, so no explicit NIC pin is needed.
          - { name: NCCL_SOCKET_IFNAME, value: "eth0" }
          - { name: NCCL_CROSS_NIC,     value: "0" }
          # --- GKE NCCL/GIB plugin + driver libs are host-mounted (volumes below) ---
          # No LD_LIBRARY_PATH override needed: the image default already carries
          # /opt/nvidia/nvda_nixl/lib64 (so NIXL telemetry loads), and the GPU runtime
          # resolves the host-mounted driver libs.
          - { name: NCCL_DEBUG,         value: "WARN" }
          # --- Dynamo / vLLM ---
          - { name: DYN_HEALTH_CHECK_ENABLED, value: "false" }
          - { name: HF_HOME,                  value: /shared-models }
          - { name: VLLM_LOGGING_LEVEL,       value: "INFO" }
          volumeMounts:
          - { name: gib,    mountPath: /usr/local/gib }
          - { name: nvidia, mountPath: /usr/local/nvidia }
          # Mount model PVC at container level (service-level mount gets overridden).
          - { name: shared-model-cache, mountPath: /shared-models }
        volumes:
        - { name: gib,    hostPath: { path: /home/kubernetes/bin/gib } }
        - { name: nvidia, hostPath: { path: /home/kubernetes/bin/nvidia } }
        - { name: shared-model-cache, persistentVolumeClaim: { claimName: shared-model-cache } }
      replicas: 1
      resources:
        limits:
          gpu: '4'
          custom:
            networking.gke.io.networks/rdma-0: "1"
            networking.gke.io.networks/rdma-1: "1"
            networking.gke.io.networks/rdma-2: "1"
            networking.gke.io.networks/rdma-3: "1"
            networking.gke.io.networks/rdma-0.IP: "1"
            networking.gke.io.networks/rdma-1.IP: "1"
            networking.gke.io.networks/rdma-2.IP: "1"
            networking.gke.io.networks/rdma-3.IP: "1"
        requests:
          gpu: '4'
          custom:
            ephemeral-storage: "2Gi"
            networking.gke.io.networks/rdma-0: "1"
            networking.gke.io.networks/rdma-1: "1"
            networking.gke.io.networks/rdma-2: "1"
            networking.gke.io.networks/rdma-3: "1"
            networking.gke.io.networks/rdma-0.IP: "1"
            networking.gke.io.networks/rdma-1.IP: "1"
            networking.gke.io.networks/rdma-2.IP: "1"
            networking.gke.io.networks/rdma-3.IP: "1"
      subComponentType: prefill

    VllmDecodeWorker:
      componentType: worker
      envFromSecret: hf-token-secret
      extraPodMetadata:
        annotations:
          prometheus.io/scrape: "true"
          prometheus.io/port: "19090"
          prometheus.io/path: "/metrics"
          networking.gke.io/default-interface: "eth0"
          networking.gke.io/interfaces: |
            [{"interfaceName":"eth0","network":"default"},
             {"interfaceName":"eth2","network":"rdma-0"},
             {"interfaceName":"eth3","network":"rdma-1"},
             {"interfaceName":"eth4","network":"rdma-2"},
             {"interfaceName":"eth5","network":"rdma-3"}]
      extraPodSpec:
        imagePullSecrets:
        - name: nvcr-pull-secret
        tolerations:
        - { key: nvidia.com/gpu,                            operator: Exists, effect: NoSchedule }
        - { key: kubernetes.io/arch,                        operator: Equal, value: arm64, effect: NoSchedule }
        mainContainer:
          command: [python3, -m, dynamo.vllm]
          image: nvcr.io/nvidia/ai-dynamo/vllm-runtime:1.1.1
          workingDir: /workspace
          args:
          # Loaded by HF model id; the model is cached on the shared PVC (lustre).
          - --model
          - Qwen/Qwen3-32B
          - --served-model-name
          - Qwen/Qwen3-32B
          - --disaggregation-mode
          - decode
          - --kv-transfer-config
          - '{"kv_connector":"NixlConnector","kv_role":"kv_both"}'
          - --tensor-parallel-size
          - '4'
          - --no-enable-log-requests
          - --gpu-memory-utilization
          - '0.90'
          - --no-enable-prefix-caching
          - --async-scheduling
          - --block-size
          - '64'
          - --hf-overrides
          - '{"rope_scaling":{"rope_type":"yarn","factor":4.0,"original_max_position_embeddings":32768},"max_position_embeddings":131072}'
          - --max-model-len
          - '131072'
          securityContext:
            privileged: true
            runAsUser: 0
            capabilities:
              add: [IPC_LOCK, SYS_PTRACE]
          env:
          - { name: NIXL_TELEMETRY_ENABLE,          value: "y" }
          - { name: NIXL_TELEMETRY_EXPORTER,        value: "prometheus" }
          - { name: NIXL_TELEMETRY_PROMETHEUS_PORT, value: "19090" }
          # NOTE: no UCX_TLS and no UCX_NET_DEVICES — see prefill block; let UCX auto-probe.
          - { name: NCCL_SOCKET_IFNAME, value: "eth0" }
          - { name: NCCL_CROSS_NIC,     value: "0" }
          # No LD_LIBRARY_PATH override — see prefill block.
          - { name: NCCL_DEBUG,         value: "WARN" }
          - { name: DYN_HEALTH_CHECK_ENABLED, value: "false" }
          - { name: HF_HOME,                  value: /shared-models }
          - { name: VLLM_LOGGING_LEVEL,       value: "INFO" }
          volumeMounts:
          - { name: gib,    mountPath: /usr/local/gib }
          - { name: nvidia, mountPath: /usr/local/nvidia }
          # Mount model PVC at container level (service-level mount gets overridden).
          - { name: shared-model-cache, mountPath: /shared-models }
        volumes:
        - { name: gib,    hostPath: { path: /home/kubernetes/bin/gib } }
        - { name: nvidia, hostPath: { path: /home/kubernetes/bin/nvidia } }
        - { name: shared-model-cache, persistentVolumeClaim: { claimName: shared-model-cache } }
      replicas: 1
      resources:
        limits:
          gpu: '4'
          custom:
            networking.gke.io.networks/rdma-0: "1"
            networking.gke.io.networks/rdma-1: "1"
            networking.gke.io.networks/rdma-2: "1"
            networking.gke.io.networks/rdma-3: "1"
            networking.gke.io.networks/rdma-0.IP: "1"
            networking.gke.io.networks/rdma-1.IP: "1"
            networking.gke.io.networks/rdma-2.IP: "1"
            networking.gke.io.networks/rdma-3.IP: "1"
        requests:
          gpu: '4'
          custom:
            ephemeral-storage: "2Gi"
            networking.gke.io.networks/rdma-0: "1"
            networking.gke.io.networks/rdma-1: "1"
            networking.gke.io.networks/rdma-2: "1"
            networking.gke.io.networks/rdma-3: "1"
            networking.gke.io.networks/rdma-0.IP: "1"
            networking.gke.io.networks/rdma-1.IP: "1"
            networking.gke.io.networks/rdma-2.IP: "1"
            networking.gke.io.networks/rdma-3.IP: "1"
      subComponentType: decode
